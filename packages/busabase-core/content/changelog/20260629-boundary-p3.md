---
title: 2026-06-29 Boundary P3 — 8 more edge-case fixes
---

# Boundary P3 — 8 more edge-case fixes

Date: 2026-06-29
Author: Kelly Peilin Chan
AI Agent: Claude

## Prompts & Instructions

**Original Request:**
> base, field, records，还有什么边界要考虑？现状+风险+你的推荐方案
> 都改，测试驱动验证

**Refined Instructions:**
- P0: spaceId 隔离缺失 — createBase/createBaseField 插入时未带 spaceId，多租户下污染
- P1: 选项删除 — select/multiselect field 删除 choice 时未检查现有记录是否引用
- P1: record merge 重新校验 required — CR merge 时用当前 schema 重新验证，不是 CR 创建时的 schema
- P1: relation link 软删除级联 — 归档 record 时同步软删除 busabaseRecordLinks
- P1: 移除 hard_delete_after_retention — API 只保留 "archive" deleteMode
- P2: node 删除级联 — mergeNodeDelete 当 node.type=base 时级联归档 base 及其所有 records
- P2: view restore API — view_restore 操作 + views.restoreChangeRequest 端点
- P2: auto-number 恢复保留 — mergeRecordRestore 不重新递增，保留原始序号（有意为之）

## What Changed
- `record-ops.ts`: createBase/createBaseField 加上 `spaceId: getContextSpaceId()`
- `merge/field.ts`: mergeBaseUpdateField 增加 choice removal guard（检查 active records 是否引用）
- `merge/record.ts`: mergeRecordCreate/Update 对当前 schema 重新校验 required；mergeRecordDelete 软删除 recordLinks；mergeRecordRestore 恢复 links
- `merge/base.ts`: mergeNodeDelete 级联归档 base + records
- `merge/view.ts`: 新增 mergeViewRestore
- `cr-lifecycle.ts`: 分发 view_restore / base_restore 操作
- `base-schemas.ts`: createDeleteChangeRequestInputSchema 移除 hard_delete_after_retention
- `schema.ts`: busabaseRecordLinks 加 deletedAt；busabaseOperationKindEnum 加 view_restore
- 新增 migration: `0002_flowery_scourge.sql`
- 新增测试: `boundary-p3.test.ts` (8 tests)

## Files Affected
- `packages/busabase-core/src/domains/base/logic/record-ops.ts`
- `packages/busabase-core/src/domains/base/logic/merge/field.ts`
- `packages/busabase-core/src/domains/base/logic/merge/record.ts`
- `packages/busabase-core/src/domains/base/logic/merge/base.ts`
- `packages/busabase-core/src/domains/base/logic/merge/view.ts`
- `packages/busabase-core/src/logic/cr-lifecycle.ts`
- `packages/busabase-core/src/logic/base-schemas.ts`
- `packages/busabase-core/src/domains/base/schema.ts`
- `apps/busabase/src/db/migrations/0002_flowery_scourge.sql`
- `packages/busabase-core/tests/boundary-p3.test.ts`

## Breaking Changes
- `createDeleteChangeRequestInputSchema` 移除 `hard_delete_after_retention` — 现有调用方只能传 `"archive"`

## Testing
- `pnpm --filter busabase-core test` → 309/309 tests pass
- boundary-p3.test.ts 覆盖全部 8 个边界场景

## Follow-up Tasks
- busabase-mobile / buda 同步 deleteMode API 变更
- choice removal 应考虑 soft-delete choice（保留历史数据）而非直接拒绝（P3+）
