---
title: 2026-06-29 Boundary P4 — Backend + UI fixes
---

# Boundary P4 — Backend + UI fixes

Date: 2026-06-29
Author: AI Assistant
AI Agent: Claude

## Prompts & Instructions

**Original Request:**
> 全都做 (implement all identified P4 boundary fixes)

**Refined Instructions:**
- Fix 8 backend boundary gaps (soft-delete filters, spaceId isolation, CR conflict status, cross-base reorder guard)
- Add UI for archived bases, deleted fields restore, conflict badge
- Wire up api-client methods for restore operations

## What Changed

### Backend fixes
- `listRecordLinks`: now filters `isNull(deletedAt)` — soft-deleted links excluded
- `listViews` (global): inner-joins bases, excludes `archivedAt IS NOT NULL`
- `addField` merge: position count respects `deletedAt`; inserts carry correct `spaceId`
- `reorderFields` merge: validates all fieldIds belong to `baseId`, throws BAD_REQUEST on mismatch
- `projectCommitFields`: fieldValues inserts now include `spaceId: getContextSpaceId()`
- `mergeChangeRequest`: catches `ORPCError("CONFLICT")`, sets CR status to `"conflict"`, re-throws
- `busabaseChangeRequestStatusEnum`: added `"conflict"` variant + migration generated
- Added `listArchivedBases()` and `listDeletedBaseFields(baseId)` query helpers
- Contract: added `listArchived`, `get`, `listDeletedFields` routes; `listViews.baseId` now optional

### UI layer
- `dashboard-shell.tsx`: "Archived" nav item (Archive icon) in scrollable section
- `archived-bases.tsx` (new): `ArchivedBasesView` — lists archived bases with per-item Restore button + error state
- `base-views.tsx`: `BaseSetupView` extended with `deletedFields` + `onRestoreField`; shows "Deleted Fields" panel with restore; form errors now show affected record IDs
- `dashboard/index.tsx`: wires `archivedBasesQuery`, `deletedFieldsQuery`, `submitRestoreBase`, `submitRestoreField`; `/archived` route renders `ArchivedBasesView`
- `change-request.ts` helpers: `statusTone` + `changeRequestStatusLabel` handle `"conflict"` (red badge)
- `api-client/index.ts`: added `createRestoreBaseChangeRequest` and `createRestoreFieldChangeRequest` to interface + implementation

### Tests
- `tests/boundary-p4.test.ts` (new): 8 integration tests covering all P4 backend fixes

## Files Affected
- `src/domains/base/logic/merge/field.ts`
- `src/domains/base/logic/queries.ts`
- `src/logic/field-values.ts`
- `src/logic/cr-lifecycle.ts`
- `src/db/schema.ts`
- `src/domains/base/contract/routes.ts`
- `src/domains/dashboard/helpers/change-request.ts`
- `src/domains/dashboard/components/dashboard-shell.tsx`
- `src/domains/dashboard/components/archived-bases.tsx` (new)
- `src/domains/dashboard/components/base-views.tsx`
- `src/domains/dashboard/hooks/use-dashboard-routes.ts`
- `src/domains/dashboard/routes.tsx`
- `src/domains/dashboard/index.tsx`
- `src/api-client/index.ts`
- `tests/boundary-p4.test.ts` (new)

## Breaking Changes
- `busabaseChangeRequestStatusEnum` gains `"conflict"` — requires DB migration in consuming apps

## Testing
- Run `pnpm test` in `packages/busabase-core` — 317+ tests pass
- Navigate to `/archived` in the dashboard to see archived bases
- Archive a base, then restore it via the Archived view
- Trigger a merge conflict to see red "Conflict" badge on CR

## Follow-up Tasks
- View archive/restore UI (context menu on view tabs)
- Record restore UI (archived records panel)
